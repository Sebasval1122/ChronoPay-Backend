from usuarios.models import Usuario
from sucursales.models import Sucursal
from django.db import models

class Asistencia:
    if Usuario is not Usuario.sucursal:
        print("Registre al usuario primero o sucursal equivocada")
    else:

        pass